/**
 * Terminal shell helpers — re-exported from @forge/platform so desktop code
 * can import locally while sharing implementations with other apps.
 */
export {
  clampTerminalSize,
  resolveDefaultShell,
  resolvePipeShell,
  resolveSpawnCwd,
  type ResolvedShell,
} from "@forge/platform";
