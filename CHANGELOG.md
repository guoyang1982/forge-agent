# Changelog

## 2026-06 — 桌面端会话体验 + Agent 多任务能力

详见 [docs/agent-capabilities.md](docs/agent-capabilities.md)。

### Agent 能力
- **子代理委派**:`spawn_agent` 把独立子任务派给隔离的嵌套代理,返回总结(深度上限 1、步数 ≤20、共享取消信号)。
- **并行执行**:一个回合里连续的只读工具(`read_file`/`list_dir`/`grep`/`echo`)与 `spawn_agent` 并发执行;直接写类工具仍串行。结果按调用顺序回填。
- **并发写安全**:`write_file`/`write_patch`/`move`/`rename` 的整段读-改-写受**按路径写锁**保护(同文件串行不损坏、异文件并行)。⚠️ `run_command` 子进程的自定义文件 I/O **不受保护**——并行子代理须写不同文件。
- **任务清单卡片**:`update_plan` 工具驱动的实时进度卡片。
- **命令执行确认**:`ui.confirmCommands` 开启后,`run_command` 前弹卡片(拒绝/允许一次/本会话总是允许);默认关,自动化/渠道旁路,与 autoApply 解耦。

### 会话与回滚
- **检查点/回滚**:每轮运行前快照工作区(挂 `refs/forge` 防 gc + 入库);「回到此处」可仅回滚文件,或文件 + 撤回对话。
- **跨会话搜索**、**导出 Markdown**、**会话内查找(Cmd+F)**、**上下文用量指示 + 一键 /compact**。

### 输入与时间线
- 行内可折叠 diff + 词级高亮;右侧「代码 + 变动」合并视图。
- 运行中消息排队、↑ 历史(持久化)、`@` 文件引用补全、失败重试、补丁撤销、双击 Esc 停止、后台完成系统通知 + 未读标记、结论/代码块复制。

### 稳定性
- daemon 单实例锁 + 构建不匹配最多重启一次(消除无限重启循环)。
- 修复:时间线重建churn、折叠/滚动漂移、重复消息丢「开始执行」行、会话被改名、补丁应用器整块匹配、截断后残留旧结论等。
- 图标 SVG 化 + 文件树渲染去重;时间线缓存同步防抖。
