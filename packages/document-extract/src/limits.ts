/** Max file size sent for extraction (bytes). */
export const MAX_ATTACHMENT_BYTES = 20 * 1024 * 1024;

/** Max characters inlined into the user message. */
export const MAX_EXTRACTED_TEXT = 50_000;
